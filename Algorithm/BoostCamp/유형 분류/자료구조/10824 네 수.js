function solve() {
  let answer = 0;
  const first = Number(nums[0] + nums[1]);
  const second = Number(nums[2] + nums[3]);

  answer = first + second;

  return answer;
}

const nums = ["10", "20", "30", "40"];
solve("");
